# 🏗️ Technische Architektur-Übersicht

## Schichtenmodell

```
┌─────────────────────────────────────────────────────────────────┐
│                   PRÄSENTATIONSSCHICHT (UI)                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  MainWindow.xaml         │  MainView.xaml               │   │
│  │  (Avalonia Desktop UI)   │  (Avalonia Controls)         │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Data Binding
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  VIEWMODEL-SCHICHT (Binding)                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  LernplanViewModel (ReactiveUI)                          │   │
│  │  - ErzeugeNeueLernEinheitCommand                         │   │
│  │  - LadeLernEinheitenCommand                              │   │
│  │  - LöscheLernEinheitCommand                              │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Dependency Injection
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SERVICE-SCHICHT (Business Logic)              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ ┌────────────────┐  ┌────────────────┐  ┌────────────┐   │   │
│  │ │Lernplan        │  │AI Service      │  │DateiAnalyse│   │   │
│  │ │Service         │  │Service         │  │Service     │   │   │
│  │ └────────────────┘  └────────────────┘  └────────────┘   │   │
│  │ ┌────────────────┐  ┌────────────────┐  ┌────────────┐   │   │
│  │ │User Service    │  │AppSettings     │  │Logger      │   │   │
│  │ │                │  │Service         │  │Service     │   │   │
│  │ └────────────────┘  └────────────────┘  └────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Repository Pattern
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 DATENHALTUNGSSCHICHT (Data Access)              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Entity Framework Core DbContext                         │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ Repository Pattern (Generic + Spezialisiert)        │ │   │
│  │  │ - IRepository<T> / Repository<T>                    │ │   │
│  │  │ - IUserRepository / UserRepository                  │ │   │
│  │  │ - ILernEinheitRepository / LernEinheitRepository    │ │   │
│  │  │ - IPromptRepository / PromptRepository              │ │   │
│  │  │ - IGenerierteCSVRepository / GenerierteCSVRepository│ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ LINQ to Entities
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATENBANK (SQLite)                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Users │ LernEinheiten │ Prompts │ GenerierteCSVs        │   │
│  │ DateiAnalysen │ UserEinstellungen                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│  📁 ~/.local/share/lernapp.db                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Service-Interaktionen

```
┌─────────────────────────────────────────────────────────────────┐
│                    LernplanViewModel                             │
└──┬────────┬─────────────────────────────────────┬────────────────┘
   │        │                                     │
   │        │                                     │
   ▼        ▼                                     ▼
┌──────────────┐  ┌────────────────────┐  ┌─────────────────────┐
│LernplanService│  │UserService        │  │UserAppSettingsService│
└──────┬───────┘  └────────┬──────────┘  └──────────┬──────────┘
       │                   │                        │
       │                   │                        │
       ▼                   ▼                        ▼
┌──────────────┐  ┌────────────────────┐  ┌─────────────────────┐
│ILernEinheit  │  │IUserRepository     │  │IRepository<UserE..>│
│Repository    │  │                    │  │                     │
└──────┬───────┘  └────────┬──────────┘  └──────────┬──────────┘
       │                   │                        │
       └───────────────────┼────────────────────────┘
                           ▼
                 ┌──────────────────────┐
                 │ LernAppDbContext     │
                 │ (Entity Framework)   │
                 └──────────┬───────────┘
                            │
                            ▼
                   ┌──────────────────┐
                   │  SQLite Database  │
                   └──────────────────┘
```

## Datenfluss - Neue Lerneinheit erstellen

```
UI (MainWindow.xaml)
    ↓
    │ Benutzer klickt "Neue Lerneinheit"
    │
    ▼
MainWindow.axaml.cs
    ↓
    │ Add_Click Event Handler
    │
    ▼
LernplanViewModel.ErzeugeNeueLernEinheitCommand
    ↓
    │ ReactiveCommand führt aus
    │
    ▼
LernplanService.ErstelleLernEinheitAsync()
    ↓
    │ Erstellt LernEinheit-Objekt
    │
    ▼
ILernEinheitRepository.AddAsync()
    ↓
    │ LernAppDbContext.LernEinheiten.Add()
    │
    ▼
Database.SaveChangesAsync()
    ↓
    │ Speichert in SQLite
    │
    ▼
Rückgabe zur UI
    ↓
    │ ViewModel.LernEinheiten.Add()
    │
    ▼
UI aktualisiert (Databinding)
```

## Dependency Injection Container

```
Program.Services (ServiceProvider)
├── DbContext
│   └── LernAppDbContext
│
├── Repositories
│   ├── IUserRepository → UserRepository
│   ├── ILernEinheitRepository → LernEinheitRepository
│   ├── IPromptRepository → PromptRepository
│   ├── IGenerierteCSVRepository → GenerierteCSVRepository
│   └── IRepository<T> → Repository<T>
│
├── Services
│   ├── ILernplanService → LernplanService
│   ├── IAIService → AIService
│   ├── IDateiAnalyseService → DateiAnalyseService
│   ├── IUserService → UserService
│   ├── IUserAppSettingsService → UserAppSettingsService
│   └── ILernAppLogger → ConsoleLogger
│
└── ViewModels
    └── LernplanViewModel (Instanzierung mit allen Dependencies)
```

## Entity-Relationen (ER-Diagramm)

```
┌─────────────────────┐
│      User           │
├─────────────────────┤
│ Id (PK)             │ 1
│ Name                ├──────────────────┐
│ Email (UNIQUE)      │                  │ n
│ PasswordHash        │              ┌───────────────────┐
│ LernzeitProTag      │              │  LernEinheit      │
│ ErstelltAm          │              ├───────────────────┤
│ AktualisiertAm      │              │ Id (PK)           │
└─────────────────────┘              │ UserId (FK)       │
         1 │                          │ Fach              │
           │                          │ Thema             │
           │ 1:1                      │ Beschreibung      │
           │                          │ Datum             │
        ┌──┴──────────────────┐       │ ErstelltAm        │
        │                     │       │ AktualisiertAm    │
        ▼                     ▼       └───────┬───────────┘
┌──────────────────┐    ┌────────────┐       │
│ UserEinstellung  │    │   Prompt   │       │ 1:n
├──────────────────┤    ├────────────┤       │
│ Id (PK)          │    │ Id (PK)    │       │
│ UserId (FK)      │    │ UserId (FK)├───────┤
│ Sprache          │    │ Text       │       │
│ Thema            │    │ Response   │       │
│ Benachrichti..   │    │ ErstelltAm │       │
│ AIModell         │    │ Kategorie  │       │
│ AktualisiertAm   │    └────┬───────┘       │
└──────────────────┘         │               ▼
                             │ 1:0..1   ┌──────────────────┐
                             │          │ DateiAnalyse     │
                             │          ├──────────────────┤
                             │          │ Id (PK)          │
                        ┌────┴─────┐    │ LernEinheitId(FK)│
                        │           │    │ Dateiname        │
                        ▼           ▼    │ InhaltZusamm...  │
                 ┌──────────────────┐    │ AnalysiertAm     │
                 │ GenerierteCSV    │    │ DateityP         │
                 ├──────────────────┤    └──────────────────┘
                 │ Id (PK)          │
                 │ UserId (FK)      │
                 │ PromptId (FK)    │
                 │ Dateiname        │
                 │ Inhalt           │
                 │ ErstelltAm       │
                 │ Beschreibung     │
                 └──────────────────┘
```

## Externe Schnittstellen

```
┌──────────────────────┐
│ LernApp (Desktop)    │
└──────────────────────┘
         │
         │ (Zukünftig)
         ▼
    ┌─────────────────┐
    │  Python Script  │
    │  (KI-Service)   │
    └─────────────────┘
         │
         │ (Optional)
         ▼
    ┌─────────────────┐
    │ OpenAI API      │
    │ Claude API      │
    │ Etc.            │
    └─────────────────┘

┌──────────────────────┐
│ ASP.NET Core Web API │ (Geplant)
├──────────────────────┤
│ /api/users           │
│ /api/lerneinheiten   │
│ /api/prompts         │
│ /api/settings        │
└──────────────────────┘
         │
         └─────────────────┐
                           │
┌──────────────────────────▼─────────┐
│ Web Frontend (React/Vue/Angular)   │
└────────────────────────────────────┘
```

## Deployment-Szenarien

### Desktop (Aktuell)
```
Windows/Linux/macOS
    ↓
LernApp.exe / dotnet LernApp.dll
    ↓
Avalonia UI
    ↓
Services Layer
    ↓
Entity Framework
    ↓
SQLite (lokal)
```

### Web (Geplant)
```
Cloud (Azure/AWS)
    ↓
ASP.NET Core Web API
    ↓
Services Layer
    ↓
Entity Framework
    ↓
SQLite / SQL Server / PostgreSQL
```

---

Dieses Architektur-Design ermöglicht:
- ✅ Wartbarkeit
- ✅ Testbarkeit
- ✅ Skalierbarkeit
- ✅ Code-Wiederverwendung (Shared Services)
- ✅ Flexibilität für verschiedene Frontend-Clients

